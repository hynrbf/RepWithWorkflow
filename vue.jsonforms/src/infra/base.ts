//base code something like this
//just an example only
export const sampleNameof = <T>(name: keyof T) => name;

export enum OnboardingType {
  Firm = "Firm",
  Ar = "Ar",
  Employee = "Employee",
  Provider = "Provider",
  Introducer = "Introducer",
}
