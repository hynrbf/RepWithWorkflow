/**
 * Represents the available `action` types for the `SchedulerItemSeriesAction` objects.
 *
 * The available actions are:
 * - `SERIES_RESET`
 * - `SERIES_SET`
 * - `SERIES_TOGGLE`
 */
export var SERIES_ACTION;
(function (SERIES_ACTION) {
  SERIES_ACTION["reset"] = "SERIES_RESET";
  SERIES_ACTION["set"] = "SERIES_SET";
  SERIES_ACTION["toggle"] = "SERIES_TOGGLE";
})(SERIES_ACTION || (SERIES_ACTION = {}));
/** @hidden */
export var seriesReducer = function seriesReducer(state, action) {
  switch (action.type) {
    case SERIES_ACTION.reset:
      return null;
    case SERIES_ACTION.set:
      return action.payload;
    case SERIES_ACTION.toggle:
      return !state;
    default:
      return state;
  }
};
/** @hidden */
export var useSeries = function useSeries(_defaultProp, _prop, _callback) {
  // const [series, setSeries] = useControlledState<DataItem | null>(defaultProp, prop, callback);
  // const handleSeriesAction = (action: SchedulerItemSeriesAction) => {
  //     const newSeries = seriesReducer(series, action);
  //     setSeries(newSeries);
  // };
  // return [series, setSeries, handleSeriesAction];
  return [];
};