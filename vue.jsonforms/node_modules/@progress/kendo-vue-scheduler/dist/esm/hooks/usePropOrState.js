import { noop } from '@progress/kendo-vue-common';
/** @hidden */
export var usePropOrState = function usePropOrState(prop, defaultValue) {
  var state = defaultValue[0],
    setState = defaultValue[1];
  return prop !== undefined ? [prop, noop] : [state, setState];
};