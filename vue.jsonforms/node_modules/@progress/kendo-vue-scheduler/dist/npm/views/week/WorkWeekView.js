"use strict";

var __rest = undefined && undefined.__rest || function (s, e) {
  var t = {};
  for (var p in s) {
    if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0) t[p] = s[p];
  }
  if (s != null && typeof Object.getOwnPropertySymbols === "function") for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
    if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i])) t[p[i]] = s[p[i]];
  }
  return t;
};
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.WorkWeekViewVue2 = exports.WorkWeekView = void 0;
// @ts-ignore
var Vue = require("vue");
var allVue = Vue;
var gh = allVue.h;
var isV3 = allVue.version && allVue.version[0] === '3';
var inject = allVue.inject;
var kendo_date_math_1 = require("@progress/kendo-date-math");
var main_1 = require("../../constants/main");
var MultiDayView_1 = require("../day/MultiDayView");
var main_2 = require("../../messages/main");
var main_3 = require("../../utils/main");
var kendo_vue_intl_1 = require("@progress/kendo-vue-intl");
/** @hidden */
var WorkWeekViewVue2 = {
  name: 'KendoWorkWeekView',
  // @ts-ignore
  emits: {
    dataaction: null
  },
  props: {
    name: {
      type: String,
      default: 'work-week'
    },
    slotDuration: {
      type: Number,
      default: 60
    },
    slotDivisions: {
      type: Number,
      default: 2
    },
    numberOfDays: {
      type: Number,
      default: 7
    },
    dateRange: {
      type: [Function, Object],
      default: function _default() {
        return workWeekViewDateRange;
      }
    },
    title: String,
    workWeekStart: {
      type: Number,
      default: kendo_date_math_1.Day.Monday
    },
    workWeekEnd: {
      type: Number,
      default: kendo_date_math_1.Day.Friday
    },
    selectedDateFormat: {
      type: String,
      default: '{0:D} - {1:D}'
    },
    selectedShortDateFormat: {
      type: String,
      default: '{0:d} - {1:d}'
    },
    showWorkHours: {
      type: Boolean,
      default: true
    },
    dateHeaderCell: [String, Function, Object],
    timeHeaderCell: [String, Function, Object],
    slotRender: [String, Function, Object],
    viewSlot: [String, Function, Object],
    resizeHint: [String, Function, Object],
    item: [String, Function, Object],
    viewItem: [String, Function, Object],
    editable: {
      type: [Boolean, Object],
      default: false
    }
  },
  inject: {
    kendoLocalizationService: {
      default: null
    },
    ks: {
      default: null
    }
  },
  // @ts-ignore
  setup: !isV3 ? undefined : function () {
    var v3 = !!isV3;
    return {
      v3: v3
    };
  },
  // @ts-ignore
  render: function render(createElement) {
    var h = gh || createElement;
    var localization = (0, kendo_vue_intl_1.provideLocalizationService)(this);
    var title = localization.toLanguageString(main_2.workWeekViewTitle, main_2.messages[main_2.workWeekViewTitle]);
    var dateRange = this.dateRange;
    var numberOfDays = Math.round((dateRange.end.getTime() - dateRange.start.getTime()) / kendo_date_math_1.MS_PER_DAY);
    return h(MultiDayView_1.MultiDayView, {
      name: this.$props.name,
      attrs: this.v3 ? undefined : {
        name: this.$props.name,
        dateRange: this.$props.dateRange,
        numberOfDays: numberOfDays,
        slotDuration: this.$props.slotDuration,
        slotDivisions: this.$props.slotDivisions,
        selectedDateFormat: this.$props.selectedDateFormat,
        selectedShortDateFormat: this.$props.selectedShortDateFormat,
        showWorkHours: this.$props.showWorkHours,
        title: title,
        step: main_1.DAYS_IN_WEEK_COUNT,
        slotRender: this.$props.slotRender,
        viewSlot: this.$props.viewSlot,
        resizeHint: this.$props.resizeHint,
        slotItem: this.$props.slotItem,
        item: this.$props.item,
        viewItem: this.$props.viewItem,
        editable: this.$props.editable,
        dateHeaderCell: this.$props.dateHeaderCell,
        timeHeaderCell: this.$props.timeHeaderCell
      },
      dateRange: this.$props.dateRange,
      numberOfDays: numberOfDays,
      slotDuration: this.$props.slotDuration,
      slotDivisions: this.$props.slotDivisions,
      selectedDateFormat: this.$props.selectedDateFormat,
      selectedShortDateFormat: this.$props.selectedShortDateFormat,
      showWorkHours: this.$props.showWorkHours,
      title: title,
      step: main_1.DAYS_IN_WEEK_COUNT,
      slotRender: this.$props.slotRender,
      viewSlot: this.$props.viewSlot,
      resizeHint: this.$props.resizeHint,
      slotItem: this.$props.slotItem,
      item: this.$props.item,
      viewItem: this.$props.viewItem,
      editable: this.$props.editable,
      dateHeaderCell: this.$props.dateHeaderCell,
      timeHeaderCell: this.$props.timeHeaderCell,
      onDataaction: this.handleDataAction,
      on: this.v3 ? undefined : {
        "dataaction": this.handleDataAction
      }
    });
  },
  methods: {
    handleDataAction: function handleDataAction(action) {
      this.$emit('dataaction', action);
    }
  }
};
exports.WorkWeekViewVue2 = WorkWeekViewVue2;
var firstDayInWeek = function firstDayInWeek(date, weekStartDay) {
  if (weekStartDay === void 0) {
    weekStartDay = kendo_date_math_1.Day.Sunday;
  }
  var first = date.clone();
  while (first.getDay() !== weekStartDay) {
    first = first.addDays(-1);
  }
  return first;
};
var workWeekViewDateRange = function workWeekViewDateRange(_a) {
  var intl = _a.intl,
    date = _a.date,
    timezone = _a.timezone,
    other = __rest(_a, ["intl", "date", "timezone"]);
  var normalized = kendo_date_math_1.ZonedDate.fromLocalDate(date, timezone);
  var workWeekStart = other.workWeekStart !== undefined ? other.workWeekStart : kendo_date_math_1.Day.Monday;
  var workWeekEnd = other.workWeekEnd !== undefined ? other.workWeekEnd : kendo_date_math_1.Day.Friday;
  var days = workWeekEnd < workWeekStart ? main_1.DAYS_IN_WEEK_COUNT + workWeekEnd - workWeekStart + 1 : workWeekEnd - workWeekStart + 1;
  var firstDate = (0, kendo_date_math_1.getDate)(firstDayInWeek(normalized, workWeekStart));
  var lastDate = (0, kendo_date_math_1.addDays)(firstDate, days);
  var zonedStart = kendo_date_math_1.ZonedDate.fromUTCDate((0, main_3.toUTCDateTime)(firstDate), timezone);
  var zonedEnd = kendo_date_math_1.ZonedDate.fromUTCDate((0, main_3.toUTCDateTime)(lastDate), timezone);
  var start = new Date(zonedStart.getTime());
  var end = new Date(zonedEnd.getTime());
  return {
    start: start,
    end: end,
    zonedStart: zonedStart,
    zonedEnd: zonedEnd
  };
};
/** @hidden */
var WorkWeekView = WorkWeekViewVue2;
exports.WorkWeekView = WorkWeekView;