import { Group, Range, Slot, DateRange } from '../models/main';
/** @hidden */
export declare const toSlots: (_dateRange: DateRange, { step }: {
    step: any;
}, { groups, ranges }: {
    groups: Group[];
    ranges: Range[];
}) => Slot[];
