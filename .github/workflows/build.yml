on: push
  # push:
  #   paths:
  #     - vue.jsonforms/**
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vue.jsonforms
    steps:
      - uses: actions/checkout@v4
      - run: sudo npm install -g --unsafe-perm=true --allow-root eslint
      - run: sudo npm run lint